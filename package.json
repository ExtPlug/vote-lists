{
  "name": "extplug-vote-lists",
  "version": "0.1.1",
  "license": "MIT",
  "description": "ExtPlug plugin that shows a list of users when hovering vote buttons.",
  "keywords": [
    "extplug-plugin"
  ],
  "dependencies": {
    "backbone-filtered-collection": "^0.4.0"
  },
  "scripts": {
    "build": "extplug bundle src/main.js build/vote-lists.js"
  },
  "devDependencies": {
    "extplug-cli": "^1.1.0"
  }
}
